<%- include('../partials/admin-head', { title }) %>
<%- include('../partials/admin-sidebar') %>

<main class="lg:pl-72">
  <div class="mx-auto max-w-7xl px-5 py-8 lg:px-8">
    <%- include('../partials/admin-flash') %>
    <div class="flex flex-col justify-between gap-6 md:flex-row md:items-end">
      <div>
        <p class="eyebrow">Blog Management</p>
        <h1 class="mt-3 font-display text-4xl font-bold text-white">Posts</h1>
        <p class="mt-2 text-slate-400">Create, edit, publish, and delete TechSphere articles.</p>
      </div>
      <a href="/admin/posts/new" class="btn-primary">Create New Post</a>
    </div>

    <section class="admin-panel mt-8 overflow-hidden">
      <div class="overflow-x-auto">
        <table class="admin-table">
          <thead>
            <tr><th>Title</th><th>Category</th><th>Status</th><th>Featured</th><th>Date</th><th>Actions</th></tr>
          </thead>
          <tbody>
            <% posts.forEach(post => { %>
              <tr>
                <td class="min-w-64"><%= post.title %></td>
                <td><%= post.category %></td>
                <td><span class="status-pill"><%= post.status %></span></td>
                <td><%= post.featured ? 'Yes' : 'No' %></td>
                <td><%= new Date(post.createdAt).toLocaleDateString() %></td>
                <td>
                  <div class="flex gap-2">
                    <a href="/blog/<%= post.slug || post._id %>" target="_blank" class="table-action">View</a>
                    <a href="/admin/posts/<%= post._id %>/edit" class="table-action">Edit</a>
                    <form action="/admin/posts/<%= post._id %>/delete" method="POST" onsubmit="return confirm('Delete this post?')">
                      <button class="table-action-danger" type="submit">Delete</button>
                    </form>
                  </div>
                </td>
              </tr>
            <% }) %>
            <% if (!posts.length) { %><tr><td colspan="6">No posts created yet.</td></tr><% } %>
          </tbody>
        </table>
      </div>
    </section>
  </div>
</main>

<%- include('../partials/admin-footer') %>
